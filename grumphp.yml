parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        composer: []
        phplint: []
        phpstan:
            level: 7
        composer_require_checker: []
        phpcs:
            standard: PSR2
        phpmd:
            ruleset: ['cleancode', 'codesize', 'design', 'unusedcode', 'naming']
        phpunit: []
        infection:
            threads: 4
